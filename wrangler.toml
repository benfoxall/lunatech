name = "lunatech"
main = "src/worker.ts"
compatibility_date = "2024-01-01"

routes = [
  { pattern = "track.benjaminbenben.com", custom_domain = true }
]

[observability]
enabled = true

# Serve built frontend assets from public directory using Workers Assets
# This is compatible with versioned deployments (wrangler versions upload)
[assets]
directory = "public"

# Enable preview deployments
[env.preview]
name = "lunatech-preview"
